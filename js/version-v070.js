// LEGEND v0.7.0 — Interface & Encounter Update build label
// Loaded after previous build labels so the live game reports the current weekend playtest version.
(() => {
  if(window.LEGEND_DATA){
    window.LEGEND_DATA.VERSION = 'v0.7.0';
  }
})();